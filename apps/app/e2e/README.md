# App E2E

The Playwright specs live at `../../tests/e2e` so they can cover routes across the app package
without being coupled to Expo Router source directories.

Run from the repo root:

```sh
pnpm test:e2e
```
